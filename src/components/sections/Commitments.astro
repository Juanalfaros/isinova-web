---
// src/components/Commitments.astro
// No necesitamos props por ahora, pero aquí podrías recibir títulos o textos desde tu CMS si quisieras.
---

<section id="garantias" class="quality-standards">
    <div class="container">
        <div class="section-header">
            <span class="section-badge">Nuestro Compromiso</span>

            <h2 class="section-title">
                Estándares de <span class="highlight">Nivel Enterprise</span>
            </h2>
            <p class="section-subtitle">
                No solo desarrollamos software, garantizamos la continuidad
                operativa de su institución con acuerdos de nivel de servicio
                (SLA) claros.
            </p>
        </div>

        <div class="standards-grid">
            <div class="standard-card">
                <div class="card-icon-wrapper">
                    <i class="ph-duotone ph-shield-check" aria-hidden="true"
                    ></i>
                </div>
                <h3 class="card-title">Seguridad y Privacidad</h3>
                <p class="card-text">
                    Desarrollo bajo principios de <strong
                        >Security by Design</strong
                    >. Cumplimiento estricto de normativas de protección de
                    datos y encriptación end-to-end en todas las integraciones.
                </p>
                <div class="card-feature">
                    <i class="ph-bold ph-check-circle"></i>
                    <span>Auditoría de vulnerabilidades</span>
                </div>
            </div>

            <div class="standard-card">
                <div class="card-icon-wrapper">
                    <i class="ph-duotone ph-chart-line-up" aria-hidden="true"
                    ></i>
                </div>
                <h3 class="card-title">Alta Concurrencia</h3>
                <p class="card-text">
                    Infraestructura diseñada para <strong
                        >escalar automáticamente</strong
                    >
                    y soportar picos de tráfico masivo (exámenes, matrículas) sin
                    caídas ni latencia perceptible.
                </p>
                <div class="card-feature">
                    <i class="ph-bold ph-check-circle"></i>
                    <span>Auto-scaling garantizado</span>
                </div>
            </div>

            <div class="standard-card">
                <div class="card-icon-wrapper">
                    <i class="ph-duotone ph-lifebuoy" aria-hidden="true"></i>
                </div>
                <h3 class="card-title">Soporte Especializado</h3>
                <p class="card-text">
                    Trato directo con ingenieros, sin bots. Ofrecemos tiempos de
                    respuesta (SLA) definidos por contrato para incidencias
                    críticas en producción.
                </p>
                <div class="card-feature">
                    <i class="ph-bold ph-check-circle"></i>
                    <span>SLA de respuesta prioritaria</span>
                </div>
            </div>
        </div>

        <div class="section-cta">
            <p class="cta-label">
                Construyamos una infraestructura sólida desde el primer día
            </p>
            <a href="/contacto" class="btn-primary">
                Solicitar Propuesta Técnica
                <i class="ph-bold ph-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<style>
    /* --- VARIABLES LOCALES (Scoped) --- */
    .quality-standards {
        --bg-card: #ffffff;
        --text-main: #0f172a;
        --text-muted: #64748b;
        --accent-color: #6246ea; /* Tu color de marca */
        --success-color: #10b981;

        padding: 6rem 0;
    }

    .container {
        max-width: 1100px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    /* --- HEADER --- */
    .section-header {
        text-align: center;
        margin-bottom: 4rem;
    }

    .section-badge {
        display: inline-block;
        padding: 0.35rem 1rem;
        border-radius: 50px;
        background: rgba(98, 70, 234, 0.1);
        color: var(--accent-color);
        font-weight: 600;
        font-size: 0.85rem;
        margin-bottom: 1.2rem;
        letter-spacing: 0.02em;
    }

    .section-title {
        font-size: 2.5rem;
        font-weight: 800;
        color: var(--text-main);
        line-height: 1.2;
        margin-bottom: 1rem;
    }

    /* Gradiente en el texto "Nivel Enterprise" */
    .highlight {
        background: linear-gradient(90deg, var(--accent-color), #8b5cf6);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .section-subtitle {
        font-size: 1.125rem;
        color: var(--text-muted);
        line-height: 1.6;
        max-width: 650px;
        margin: 0 auto;
    }

    /* --- GRID --- */
    .standards-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 2rem;
        margin-bottom: 4rem;
    }

    @media (min-width: 992px) {
        .standards-grid {
            grid-template-columns: repeat(3, 1fr);
        }
    }

    /* --- CARDS --- */
    .standard-card {
        background: var(--bg-card);
        border: 1px solid #e2e8f0;
        border-radius: 1.25rem; /* Bordes más redondeados */
        padding: 2rem;
        display: flex;
        flex-direction: column;
        transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        position: relative;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05);
    }

    .standard-card:hover {
        transform: translateY(-8px);
        box-shadow:
            0 20px 25px -5px rgba(0, 0, 0, 0.1),
            0 10px 10px -5px rgba(0, 0, 0, 0.04);
        border-color: rgba(98, 70, 234, 0.3);
    }

    /* Icono Header */
    .card-icon-wrapper {
        width: 56px;
        height: 56px;
        background: rgba(98, 70, 234, 0.08);
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        color: var(--accent-color);
        font-size: 2rem;
        margin-bottom: 1.5rem;
        transition: transform 0.3s ease;
    }

    .standard-card:hover .card-icon-wrapper {
        transform: scale(1.1) rotate(5deg);
        background: var(--accent-color);
        color: white;
    }

    .card-title {
        font-size: 1.25rem;
        font-weight: 700;
        color: var(--text-main);
        margin-bottom: 0.75rem;
    }

    .card-text {
        font-size: 0.95rem;
        line-height: 1.6;
        color: var(--text-muted);
        margin-bottom: 2rem;
        flex-grow: 1; /* Esto asegura que los checks queden alineados abajo */
    }

    /* Check Footer */
    .card-feature {
        margin-top: auto;
        padding-top: 1.25rem;
        border-top: 1px solid #f1f5f9;
        display: flex;
        align-items: center;
        gap: 0.6rem;
        font-size: 0.9rem;
        font-weight: 600;
        color: var(--text-main);
    }

    .card-feature i {
        color: var(--success-color);
        font-size: 1.25rem;
    }

    /* --- CTA FINAL --- */
    .section-cta {
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.25rem;
    }

    .cta-label {
        color: var(--text-muted);
        font-size: 1rem;
        font-weight: 500;
    }

    .btn-primary {
        display: inline-flex;
        align-items: center;
        gap: 0.75rem;
        background-color: var(--text-main);
        color: white;
        padding: 1rem 2.25rem;
        border-radius: 100px;
        font-weight: 600;
        text-decoration: none;
        transition: all 0.3s ease;
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    .btn-primary:hover {
        background-color: var(--accent-color);
        transform: translateY(-2px);
        box-shadow: 0 10px 15px -3px rgba(98, 70, 234, 0.4);
    }
</style>
