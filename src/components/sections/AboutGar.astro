---

---

<section id="nosotros" class="integrated-section bg-tech-dark">
    <div class="bg-base"></div>
    <div class="tech-grid-bg"></div>
    <div class="ambient-glow"></div>

    <div class="container">
        <div class="split-layout">
            <div class="identity-col">
                <span class="section-badge accent">Sobre Isinova</span>

                <h2 class="section-title">
                    Expertos en EdTech <br />
                    <span class="text-gradient">Made in Chile</span>
                </h2>

                <p class="about-text">
                    Somos una empresa apasionada por cerrar la brecha digital
                    educativa. Entendemos la realidad local y ofrecemos
                    soluciones tecnológicas de clase mundial, pero con la
                    cercanía y el soporte humano que tu institución realmente
                    necesita.
                </p>

                <div class="stats-row">
                    <div class="stat-item">
                        <span class="stat-value">+50</span>
                        <span class="stat-label">Instituciones</span>
                    </div>
                    <div class="stat-divider"></div>
                    <div class="stat-item">
                        <span class="stat-value">99.9%</span>
                        <span class="stat-label">Uptime</span>
                    </div>
                    <div class="stat-divider"></div>
                    <div class="stat-item">
                        <span class="stat-value">24/7</span>
                        <span class="stat-label">Soporte</span>
                    </div>
                </div>
            </div>

            <div class="guarantees-col">
                <div class="guarantees-header">
                    <span class="section-badge accent">Nuestra Promesa</span>
                    <h3>
                        Tecnología de <span class="highlight"
                            >Alta Confianza</span
                        >
                    </h3>
                    <p>
                        No solo entregamos código, entregamos tranquilidad. Cada
                        proyecto está respaldado por compromisos claros.
                    </p>
                </div>

                <div class="cards-stack">
                    <div class="feature-card">
                        <div class="icon-box accent">
                            <i class="ph-fill ph-seal-check"></i>
                        </div>
                        <div class="text-box">
                            <strong>Garantía Técnica de 30 Días</strong>
                            <span
                                >Corrección inmediata de cualquier detalle tras
                                el lanzamiento, sin costo adicional.</span
                            >
                        </div>
                    </div>

                    <div class="feature-card">
                        <div class="icon-box primary">
                            <i class="ph-fill ph-clock-countdown"></i>
                        </div>
                        <div class="text-box">
                            <strong>Soporte en &lt; 24 Horas</strong>
                            <span
                                >Tiempo de respuesta garantizado para mantener
                                tu plataforma siempre operativa.</span
                            >
                        </div>
                    </div>

                    <div class="feature-card">
                        <div class="icon-box secure">
                            <i class="ph-fill ph-shield-checkered"></i>
                        </div>
                        <div class="text-box">
                            <strong>Seguridad Nivel Bancario</strong>
                            <span
                                >Cifrado SSL, backups diarios y protección
                                contra ataques DDoS incluidos.</span
                            >
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
    .integrated-section {
        --bg-surface: #1e293b; /* Slate 800 */
        --border: rgba(255, 255, 255, 0.08);
        --text-heading: #f8fafc;
        --text-body: #94a3b8;
        --accent: #00ff8c; /* Verde Neon */

        padding: 8rem 0;
    }

    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    /* LAYOUT PRINCIPAL */
    .split-layout {
        display: grid;
        grid-template-columns: 1fr;
        gap: 4rem;
        align-items: center;
    }

    @media (min-width: 992px) {
        .split-layout {
            grid-template-columns: 1fr 1fr;
            gap: 6rem;
        }
    }

    /* --- COLUMNA IZQUIERDA (IDENTIDAD) --- */
    .identity-col {
        max-width: 600px;
    }

    .section-title {
        font-size: 2.5rem;
        font-weight: 800;
        line-height: 1.1;
        color: var(--text-heading);
        margin-bottom: 1.5rem;
    }

    .text-gradient {
        background: linear-gradient(135deg, var(--primary), #a855f7);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .about-text {
        font-size: 1.125rem;
        line-height: 1.7;
        margin-bottom: 3rem;
    }

    /* Stats Row */
    .stats-row {
        display: flex;
        align-items: center;
        justify-content: space-between;
        background: rgba(255, 255, 255, 0.03);
        border: 1px solid var(--border);
        padding: 1.5rem 2rem;
        border-radius: 1rem;
    }

    .stat-item {
        text-align: center;
    }
    .stat-value {
        display: block;
        font-size: 1.75rem;
        font-weight: 800;
        color: var(--text-heading);
        line-height: 1;
        margin-bottom: 4px;
    }
    .stat-label {
        font-size: 0.8rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.05em;
    }
    .stat-divider {
        width: 1px;
        height: 40px;
        background: var(--border);
    }

    /* --- COLUMNA DERECHA (GARANTÍAS) --- */
    .guarantees-col {
        background: var(--bg-surface);
        padding: 2.5rem;
        border-radius: 2rem;
        border: 1px solid var(--border);
        box-shadow: 0 20px 40px -10px rgba(0, 0, 0, 0.3);
        position: relative;
        overflow: hidden;
    }

    /* Brillo superior en la tarjeta derecha */
    .guarantees-col::before {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        height: 1px;
        background: linear-gradient(
            90deg,
            transparent,
            rgba(255, 255, 255, 0.2),
            transparent
        );
    }

    .guarantees-header {
        margin-bottom: 2.5rem;
    }

    .guarantees-header h3 {
        font-size: 1.75rem;
        font-weight: 700;
        color: var(--text-heading);
        margin-bottom: 1rem;
    }

    .highlight {
        color: var(--accent);
        position: relative;
    }

    .guarantees-header p {
        font-size: 0.95rem;
        line-height: 1.6;
        color: var(--text-body);
        margin: 0;
    }

    /* Cards Stack (Lista de garantías) */
    .cards-stack {
        display: flex;
        flex-direction: column;
        gap: 1.25rem;
    }

    .feature-card {
        display: flex;
        align-items: flex-start;
        gap: 1rem;
        padding: 1rem;
        border-radius: 1rem;
        transition: background 0.3s ease;
    }

    .feature-card:hover {
        background: rgba(255, 255, 255, 0.03);
    }

    .icon-box {
        width: 44px;
        height: 44px;
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.5rem;
        flex-shrink: 0;
    }

    .accent {
        background: rgba(0, 255, 140, 0.1);
        color: var(--accent);
    }
    .primary {
        background: rgba(99, 102, 241, 0.1);
        color: var(--primary);
    }
    .secure {
        background: rgba(251, 191, 36, 0.1);
        color: #fbbf24;
    } /* Amarillo seguridad */

    .text-box strong {
        display: block;
        font-size: 1.05rem;
        color: var(--text-heading);
        margin-bottom: 0.25rem;
    }

    .text-box span {
        font-size: 0.9rem;
        line-height: 1.5;
        color: var(--text-body);
    }

    @media (max-width: 768px) {
        .section-title {
            font-size: 2rem;
        }
        .stats-row {
            flex-direction: column;
            gap: 1.5rem;
        }
        .stat-divider {
            width: 100%;
            height: 1px;
        }
        .guarantees-col {
            padding: 1.5rem;
        }
    }
</style>
