---

---

<section id="metodologia" class="process-section">
    <div class="tech-grid-bg"></div>
    <div class="ambient-glow"></div>

    <div class="container">
        <div class="section-header">
            <span class="section-badge accent">Process Architecture</span>
            <h2 class="section-title">
                Ingeniería de <span class="text-span gradient highlight-brand primary">Ciclo Completo</span>
            </h2>
            <p class="section-desc">
                No improvisamos. Ejecutamos una metodología de ingeniería de software 
                estricta, diseñada para mitigar riesgos y garantizar entregables 
                de calidad empresarial.
            </p>
        </div>

        <div class="timeline-wrapper">
            <div class="data-stream-line"></div>

            <div class="timeline-row">
                <div class="timeline-content left">
                    <div class="tech-card" data-step="01">
                        <div class="card-glow"></div>
                        <div class="card-body">
                            <div class="step-badge">01</div>
                            <h3>Discovery & Arquitectura</h3>
                            <p>
                                Levantamiento de requerimientos técnicos y pedagógicos. 
                                Diseñamos el diagrama de arquitectura (AWS/Cloud), 
                                definimos el stack y entregamos la Carta Gantt del proyecto.
                            </p>
                        </div>
                    </div>
                </div>
                <div class="timeline-node">
                    <div class="node-core">
                        <i class="ph-bold ph-compass-tool"></i>
                    </div>
                </div>
                <div class="timeline-spacer"></div>
            </div>

            <div class="timeline-row reverse">
                <div class="timeline-spacer"></div>
                <div class="timeline-node">
                    <div class="node-core">
                        <i class="ph-bold ph-brackets-curly"></i>
                    </div>
                </div>
                <div class="timeline-content right">
                    <div class="tech-card" data-step="02">
                        <div class="card-glow"></div>
                        <div class="card-body">
                            <div class="step-badge">02</div>
                            <h3>Desarrollo & Integración</h3>
                            <p>
                                Ejecución en sprints. Configuramos el núcleo del LMS, 
                                desarrollamos conectores API (SENCE/CRM) y aplicamos 
                                tu identidad visual con estándares UX/UI modernos.
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="timeline-row">
                <div class="timeline-content left">
                    <div class="tech-card" data-step="03">
                        <div class="card-glow"></div>
                        <div class="card-body">
                            <div class="step-badge">03</div>
                            <h3>QA & Hardening</h3>
                            <p>
                                Nadie entra a producción sin pruebas. Realizamos 
                                Tests de Carga (Stress Testing), auditoría de seguridad 
                                y validación de integridad de datos migrados.
                            </p>
                        </div>
                    </div>
                </div>
                <div class="timeline-node">
                    <div class="node-core">
                        <i class="ph-bold ph-shield-check"></i>
                    </div>
                </div>
                <div class="timeline-spacer"></div>
            </div>

            <div class="timeline-row reverse">
                <div class="timeline-spacer"></div>
                <div class="timeline-node highlight-node">
                    <div class="node-core">
                        <i class="ph-bold ph-rocket-launch"></i>
                    </div>
                </div>
                <div class="timeline-content right">
                    <div class="tech-card highlight-card" data-step="04">
                        <div class="card-glow"></div>
                        <div class="card-body">
                            <div class="step-badge">04</div>
                            <h3>Despliegue & Onboarding</h3>
                            <p>
                                Puesta en marcha "Zero-Downtime". Configuramos el 
                                monitoreo de servidores, entregamos la documentación 
                                técnica y capacitamos a tu equipo administrador.
                            </p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="cta-wrapper">
            <a href="#contacto" class="btn-glow">
                Iniciar mi Proyecto <i class="ph-bold ph-arrow-right"></i>
            </a>
        </div>
    </div>
</section>

<style>
/* --- PEGA AQUÍ TU CSS ORIGINAL (ES PERFECTO) --- */
/* Solo asegúrate de mantener las variables CSS que definiste arriba */
    /* --- 1. CONFIGURACIÓN BASE --- */
    .process-section {
        --bg-deep: #050b14;
        --card-bg: #0f172a;
        --primary: #6366f1;
        --accent: #10b981;
        --line-color: rgba(99, 102, 241, 0.15);

        position: relative;
        padding: 6rem 0;
        background-color: var(--bg-deep);
        overflow: hidden;
        color: #cbd5e1;
    }

    @media (min-width: 768px) {
        .process-section {
            padding: 9rem 0;
        }
    }

    .tech-grid-bg {
    position: absolute;
    inset: 0;
    background-image: conic-gradient(
        from 0deg at 50% 50%,
        rgba(99, 102, 241, 0.05) 0deg,
        transparent 60deg,
        rgba(168, 85, 247, 0.05) 120deg,
        transparent 180deg,
        rgba(99, 102, 241, 0.05) 240deg,
        transparent 300deg,
        rgba(168, 85, 247, 0.05) 360deg
    );
    
    mask-image: radial-gradient(
        circle at center,
        black 40%,
        transparent 80%
    );
    filter: blur(40px);
    pointer-events: none;
}

    .ambient-glow {
        position: absolute;
        top: 30%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 800px;
        height: 800px;
        background: radial-gradient(
            circle,
            rgba(99, 102, 241, 0.1) 0%,
            transparent 70%
        );
        z-index: 0;
        pointer-events: none;
    }

    .container {
        position: relative;
        z-index: 2;
        max-width: 1200px;
        margin: 0 auto;
        padding: 0 1.5rem;
    }

    /* --- 2. HEADER --- */
    .section-header {
        text-align: center;
        max-width: 750px;
        margin: 0 auto 5rem;
    }

    .section-badge {
        display: inline-block;
        font-family: monospace; /* Fuente técnica */
        font-size: 0.85rem;
        color: var(--accent);
        background: rgba(16, 185, 129, 0.1);
        padding: 6px 14px;
        border-radius: 50px;
        border: 1px solid rgba(16, 185, 129, 0.3);
        margin-bottom: 1.5rem;
        letter-spacing: 0.05em;
    }

    .section-title {
        font-size: 2.5rem;
        color: white;
        margin-bottom: 1.5rem;
        font-weight: 800;
    }

    .text-span.gradient {
        background: linear-gradient(135deg, var(--primary), var(--accent));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .section-desc {
        font-size: 1.1rem;
        color: #94a3b8;
        line-height: 1.6;
    }

    /* --- 3. TIMELINE CORE --- */
    .timeline-wrapper {
        position: relative;
        max-width: 1000px;
        margin: 0 auto;
        padding: 1rem 0;
    }

    .data-stream-line {
        position: absolute;
        left: 21px; /* Center of the node in mobile */
        top: 0;
        bottom: 0;
        width: 2px;
        background: var(--line-color);
        overflow: hidden;
        z-index: 1;
    }

    .data-stream-line::after {
        content: "";
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: linear-gradient(
            to bottom,
            transparent,
            var(--primary),
            transparent
        );
        background-size: 100% 300px;
        background-repeat: no-repeat;
        animation: dataFlow 4s linear infinite;
    }

    @keyframes dataFlow {
        0% { transform: translateY(-100%); }
        100% { transform: translateY(100%); }
    }

    .timeline-row {
        display: flex;
        flex-direction: row;
        align-items: flex-start;
        margin-bottom: 3.5rem;
        position: relative;
        width: 100%;
    }

    /* --- 4. NODOS --- */
    .timeline-node {
        width: 44px;
        height: 44px;
        flex-shrink: 0;
        z-index: 5;
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        order: 1;
    }

    .node-core {
        width: 32px;
        height: 32px;
        background: var(--bg-deep);
        border: 2px solid #334155;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1rem;
        color: #94a3b8;
        transition: all 0.4s cubic-bezier(0.23, 1, 0.32, 1);
        box-shadow: 0 0 0 5px var(--bg-deep);
    }

    .timeline-row:hover .node-core {
        border-color: var(--primary);
        color: white;
        background: var(--primary);
        box-shadow: 0 0 20px rgba(99, 102, 241, 0.4), 0 0 0 5px var(--bg-deep);
        transform: scale(1.1);
    }

    /* --- 5. TARJETAS TECH --- */
    .timeline-content {
        flex: 1;
        padding-left: 1.5rem;
        order: 2;
        min-width: 0;
    }

    .timeline-spacer { display: none; }

    .tech-card {
        position: relative;
        background: rgba(30, 41, 59, 0.3);
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-radius: 12px;
        padding: 1px;
        backdrop-filter: blur(8px);
        transition: all 0.4s ease;
    }

    .card-body {
        background: var(--card-bg);
        border-radius: 11px;
        padding: 1.75rem;
        position: relative;
        overflow: hidden;
    }

    .tech-card:hover {
        transform: translateY(-4px);
        border-color: rgba(99, 102, 241, 0.4);
        box-shadow: 0 20px 40px -15px rgba(0, 0, 0, 0.6);
    }

    .step-badge {
        font-family: monospace;
        font-size: 2rem;
        font-weight: 800;
        color: rgba(255, 255, 255, 0.03);
        position: absolute;
        top: 0.75rem;
        right: 1.25rem;
        line-height: 1;
    }

    .tech-card h3 {
        font-size: 1.2rem;
        font-weight: 700;
        color: white;
        margin-bottom: 0.75rem;
    }

    .tech-card p {
        font-size: 0.95rem;
        color: #94a3b8;
        line-height: 1.6;
        margin: 0;
    }

    .highlight-node .node-core {
        border-color: var(--accent);
        color: var(--accent);
    }

    .highlight-card:hover {
        border-color: var(--accent);
    }

    /* --- 6. CTA BUTTON --- */
    .cta-wrapper {
        text-align: center;
        margin-top: 5rem;
        position: relative;
        z-index: 5;
    }

    .btn-glow {
        display: inline-flex;
        align-items: center;
        gap: 10px;
        background: var(--primary);
        color: white;
        padding: 1rem 2.5rem;
        border-radius: 100px;
        font-weight: 700;
        font-size: 1rem;
        text-decoration: none;
        transition: all 0.3s cubic-bezier(0.23, 1, 0.32, 1);
        box-shadow: 0 0 20px rgba(99, 102, 241, 0.4);
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .btn-glow:hover {
        transform: translateY(-3px);
        background: #4f46e5;
        box-shadow: 0 0 40px rgba(99, 102, 241, 0.6);
        border-color: rgba(255, 255, 255, 0.3);
    }
    
    .btn-glow i { transition: transform 0.2s; }
    .btn-glow:hover i { transform: translateX(3px); }


    /* --- 7. DESKTOP MEDIA QUERIES --- */
    @media (min-width: 992px) {
        .data-stream-line {
            left: 50%;
            transform: translateX(-50%);
        }

        .timeline-row {
            align-items: center;
            justify-content: center;
        }

        /* Zigzag logic */
        .timeline-row.reverse { flex-direction: row; }
        .timeline-row:not(.reverse) { flex-direction: row-reverse; }

        .timeline-node { margin: 0 4rem; order: 2; }
        .timeline-content { padding: 0; flex: 1; order: 1; }

        .timeline-row:not(.reverse) .timeline-content { text-align: right; }
        .timeline-row.reverse .timeline-content { text-align: left; }

        .timeline-spacer { display: block; flex: 1; order: 3; }

        /* Conector Card Logic */
        .tech-card::before {
            content: "";
            position: absolute;
            top: 50%;
            width: 4.1rem;
            height: 1px;
            background: rgba(99, 102, 241, 0.3);
            display: block;
            z-index: -1;
        }

        .timeline-row:not(.reverse) .tech-card::before { left: 100%; }
        .timeline-row.reverse .tech-card::before { right: 100%; }

        .timeline-row:hover .tech-card::before {
            background: var(--primary);
            box-shadow: 0 0 15px var(--primary);
            height: 2px;
        }

        .timeline-row:not(.reverse) .step-badge {
            right: auto;
            left: 1.25rem;
        }
    }
</style>