---
// src/components/sections/AboutLeader.astro
import { Image } from "astro:assets";

interface Props {
    image: ImageMetadata;
}

const { image } = Astro.props;
---

<section class="section-leader bg-tech-dark" aria-labelledby="leader-title">
    <div class="bg-base"></div>
    <div class="leader-glow-bg"></div>
    
    <div class="container relative-z">
        <div class="leader-header-mobile">
            <span class="section-badge accent">Liderazgo & Visión</span>
            <h2 id="leader-title-mobile" class="section-title lg">Visión, Código y <span class="highlight-brand primary gradient3 text-span">Estrategia</span></h2>
        </div>

        <article class="leader-grid-expanded">
            <div class="leader-profile-col">
                <figure class="leader-frame-premium">
                    <div class="avatar-photo-giant cutout-mode">
                        <div class="glow-backdrop"></div>
                        <Image
                            src={image}
                            alt="Adolfo Saballa - CEO Isinova"
                            class="leader-img-premium"
                            width={800}
                            height={1000}
                            quality="max"
                            format="webp"
                            loading="eager"
                        />
                    </div>

                    <div class="badges-wrapper-responsive">
                        <div class="leader-float-badge left floating-anim">
                            <i class="ph-fill ph-chalkboard-teacher"></i> Docente AIEP
                        </div>
                        <div class="leader-float-badge right floating-anim-delayed">
                            <i class="ph-fill ph-certificate"></i> Magíster TI
                        </div>
                    </div>
                </figure>

                <aside class="leader-specs-box">
                    <h4>Arsenal Técnico</h4>
                    <ul class="tech-list-dense">
                        <li>
                            <i class="ph-bold ph-database"></i> SQL Server / Big Data
                        </li>
                        <li>
                            <i class="ph-bold ph-code"></i> .NET Core / C# / Azure
                        </li>
                        <li>
                            <i class="ph-bold ph-strategy"></i> Arquitectura LMS
                        </li>
                        <li>
                            <i class="ph-bold ph-robot"></i> Web Scraping & RPA
                        </li>
                        <li>
                            <i class="ph-bold ph-graduation-cap"></i> Moodle & Canvas Expert
                        </li>
                    </ul>
                </aside>
            </div>

            <div class="leader-content-col">
                <header class="leader-titles desktop-only">
                    <span class="section-badge accent">Liderazgo Técnico</span>
                    <h2 id="leader-title" class="section-title md">Visión, Código y <span class="highlight-brand primary gradient3 text-span">Estrategia</span></h2>
                </header>

                <div class="leader-name-block">
                    <h3>Adolfo Saballa Villegas</h3>
                    <p class="role-subtitle-main">
                        CEO & Founder | Arquitecto de Software Senior
                    </p>
                </div>

                <div class="bio-expanded">
                    <p class="lead-bio">
                        Fundador de <strong>Isinova SpA</strong>. Con más de 12 años transformando la educación digital, Adolfo lidera la infraestructura crítica que hoy soporta a más de <strong>50.000 estudiantes activos</strong>, garantizando continuidad operativa donde otros fallan.
                    </p>
                    
                    <div class="bio-block">
                        <h5>
                            <i class="ph-fill ph-check-circle accent"></i> Ingeniería de Alta Disponibilidad
                        </h5>
                        <p>
                            Especialista en ecosistemas <strong>LMS (Moodle/Canvas)</strong> a gran escala. Su enfoque de "Ingeniería de Ciclo Completo" asegura un <strong>99.9% de Uptime</strong> incluso durante procesos críticos de matrícula.
                        </p>
                    </div>
                    
                    <div class="bio-block">
                        <h5>
                            <i class="ph-fill ph-check-circle accent"></i> Visión Docente y Estratégica
                        </h5>
                        <p>
                            Como <strong>Docente de Educación Superior</strong>, entiende el dolor del usuario final. No solo entrega código; diseña ecosistemas educativos fluidos, eliminando la deuda técnica que frustra a instituciones académicas.
                        </p>
                    </div>
                </div>

                <div class="leader-credentials">
                    <div class="credential-group">
                        <span class="cred-label">Certificaciones Avanzadas</span>
                        <div class="badges-row">
                            <span class="cert-badge ibm" title="Verificado por IBM">
                                <i class="ph-bold ph-brain"></i> IBM AI & Data Science
                            </span>
                            <span class="cert-badge scrum" title="Gestión Ágil">
                                <i class="ph-bold ph-arrows-clockwise"></i> Scrum Master
                            </span>
                        </div>
                    </div>
                </div>
            </div>
        </article>
    </div>
</section>

<style>
    /* --- CONFIGURACIÓN GENERAL --- */
    .section-leader {
        /* Reducimos padding base para que no sea tan alto en desktop pequeños */
        padding: 6rem 0; 
        position: relative;
        overflow: hidden; /* Vital para que el glow no rompa el ancho del móvil */
    }
    
    .relative-z { position: relative; z-index: 10; }

    /* --- LAYOUT GRID --- */
    .leader-grid-expanded {
        display: grid;
        grid-template-columns: 380px 1fr; /* Ajustado ligeramente para laptops de 13" */
        gap: 4rem;
        align-items: start;
    }

    /* --- FOTO PREMIUM --- */
    .leader-profile-col { position: relative; }
    .leader-frame-premium { position: relative; margin: 0 0 2rem 0; }

    .avatar-photo-giant {
        width: 100%;
        height: 520px;
        border-radius: 0 0 2rem 2rem;
        position: relative;
        z-index: 1;
        background: transparent;
        display: flex;
        align-items: flex-end;
        justify-content: center;
        /* Evita que la imagen se deforme en resizes extraños */
        overflow: visible; 
    }

    /* Aura luminosa (Backdrop) */
    .glow-backdrop {
        position: absolute;
        width: 100%;
        height: 85%;
        bottom: 0;
        left: 0;
        background: radial-gradient(
            circle at center bottom,
            rgba(99, 102, 241, 0.25) 0%, 
            rgba(30, 27, 75, 0.5) 50%,   
            transparent 75%
        );
        z-index: -1;
        border-radius: 2rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        transition: all 0.5s ease;
    }

    .leader-profile-col:hover .glow-backdrop {
        background: radial-gradient(
            circle at center bottom,
            rgba(99, 102, 241, 0.4) 0%, 
            rgba(30, 27, 75, 0.7) 60%,   
            transparent 85%
        );
        box-shadow: 0 0 40px rgba(99, 102, 241, 0.15);
    }

    .leader-img-premium {
        width: 100%;
        height: 108%;
        object-fit: contain;
        object-position: bottom center;
        filter: drop-shadow(0 0 15px rgba(99, 102, 241, 0.2)) 
                drop-shadow(10px 10px 30px rgba(0, 0, 0, 0.5));
        transition: transform 0.4s ease-out, filter 0.4s;
        z-index: 2;
    }

    .leader-profile-col:hover .leader-img-premium {
        transform: scale(1.03) translateY(-8px);
        filter: drop-shadow(0 0 25px rgba(99, 102, 241, 0.5)) 
                drop-shadow(15px 15px 40px rgba(0, 0, 0, 0.7));
    }

    /* --- BADGES FLOTANTES --- */
    .leader-float-badge {
        background: #0f172a;
        color: white;
        padding: 0.7rem 1.2rem; /* Optimizado para que no sean enormes */
        border-radius: 100px;
        font-weight: 600;
        font-size: 0.85rem;
        display: flex;
        align-items: center;
        gap: 0.5rem;
        border: 1px solid rgba(255, 255, 255, 0.15);
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.4);
        z-index: 3;
        white-space: nowrap;
        transition: all 0.3s ease;
        cursor: default;
    }

    @keyframes float-left {
        0%, 100% { transform: translateY(0) rotate(-3deg); }
        50% { transform: translateY(-10px) rotate(-3deg); }
    }
    @keyframes float-right {
        0%, 100% { transform: translateY(0) rotate(3deg); }
        50% { transform: translateY(-10px) rotate(3deg); }
    }

    .leader-float-badge.left {
        position: absolute; bottom: 40px; left: -20px;
        animation: float-left 5s ease-in-out infinite;
    }
    .leader-float-badge.right {
        position: absolute; top: 50px; right: -20px;
        background: var(--primary);
        animation: float-right 5s ease-in-out infinite 1s; 
    }

    .leader-float-badge:hover {
        animation-play-state: paused;
        transform: scale(1.1) rotate(0deg) !important;
        border-color: var(--accent);
        z-index: 10;
    }

    /* --- SPECS BOX --- */
    .leader-specs-box {
        background: rgba(255, 255, 255, 0.02);
        border: 1px solid rgba(255, 255, 255, 0.05);
        border-radius: 1.5rem;
        padding: 2rem;
        margin-top: 1rem;
        transition: all 0.3s ease;
        position: relative;
        overflow: hidden;
    }
    .leader-specs-box:hover {
        background: rgba(255, 255, 255, 0.04);
        border-color: rgba(99, 102, 241, 0.3);
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0,0,0,0.2);
    }
    .leader-specs-box h4 {
        color: #94a3b8; font-size: 0.85rem; text-transform: uppercase;
        letter-spacing: 2px; margin-bottom: 1.5rem;
        border-bottom: 1px solid rgba(255, 255, 255, 0.1); padding-bottom: 0.5rem;
    }

    .tech-list-dense li {
        color: #e2e8f0; display: flex; align-items: center; gap: 0.8rem;
        margin-bottom: 1rem; font-size: 0.95rem;
        transition: transform 0.2s ease, color 0.2s;
    }
    .tech-list-dense li i {
        color: var(--primary); font-size: 1.25rem; transition: transform 0.3s;
    }
    .tech-list-dense li:hover { transform: translateX(8px); color: white; }
    .tech-list-dense li:hover i { transform: scale(1.2); color: var(--accent); }

    /* --- TEXTOS --- */
    .leader-header-mobile { display: none; text-align: center; margin-bottom: 3rem; }
    
    .leader-titles.desktop-only {
        margin-bottom: 2.5rem; border-left: 4px solid var(--accent); padding-left: 2rem;
    }
    
    .leader-name-block h3 { font-size: 2.5rem; color: white; margin-bottom: 0.25rem; line-height: 1.1;}
    .role-subtitle-main {
        color: var(--accent); font-size: 1.1rem; font-weight: 600;
        margin-bottom: 2rem; display: block; letter-spacing: 0.5px;
    }

    .lead-bio {
        font-size: 1.15rem; color: #f1f5f9; margin-bottom: 2.5rem;
        font-weight: 300; line-height: 1.7;
    }

    .bio-block { margin-bottom: 2rem; }
    .bio-block h5 {
        color: white; font-size: 1.1rem; margin-bottom: 0.5rem;
        display: flex; align-items: center; gap: 0.6rem;
    }
    .bio-block p { margin-left: 2rem; color: #94a3b8; line-height: 1.6; }

    /* --- CREDENCIALES --- */
    .leader-credentials {
        margin-top: 3rem; border-top: 1px solid rgba(255, 255, 255, 0.1); padding-top: 2rem;
    }
    .cred-label { color: #64748b; font-size: 0.85rem; margin-bottom: 1rem; display: block; text-transform: uppercase; }
    .badges-row { display: flex; gap: 1rem; flex-wrap: wrap; }
    
    .cert-badge {
        background: rgba(255, 255, 255, 0.03);
        padding: 0.6rem 1.2rem; border-radius: 8px;
        color: white; font-size: 0.9rem;
        border: 1px solid rgba(255, 255, 255, 0.1);
        display: flex; align-items: center; gap: 0.6rem;
        transition: all 0.3s ease; cursor: default;
    }
    .cert-badge:hover {
        background: rgba(255, 255, 255, 0.08);
        border-color: var(--primary);
        transform: translateY(-3px);
    }

    /* --- GLOW AMBIENTAL --- */
    .leader-glow-bg {
        position: absolute; width: 1000px; height: 1000px;
        background: radial-gradient(circle at center, rgba(99, 102, 241, 0.1) 0%, transparent 65%);
        top: -200px; right: -300px; z-index: 0; pointer-events: none;
    }

    /* =========================================
       RESPONSIVE & MOBILE
       ========================================= */
       
    @media (max-width: 1024px) {
        /* Ajuste de Padding para Tablets/Mobile */
        .section-leader { padding: 4rem 0; }
        
        /* Layout Grid -> Columna */
        .leader-grid-expanded {
            display: flex; flex-direction: column; 
            gap: 3rem; align-items: center;
        }
        
        /* Headers */
        .leader-header-mobile { display: block; }
        /* Usamos variable CSS para tamaño consistente si está disponible, sino fallback */
        /* .leader-header-mobile h2 { 
            font-size: clamp(2rem, 5vw, 2.5rem); 
            color: white; margin-top: 1rem; 
        } */
        .leader-titles.desktop-only { display: none; }
        
        /* Columna de Perfil */
        .leader-profile-col { width: 100%; max-width: 500px; }
        .avatar-photo-giant { height: 450px; }
        
        /* Alineación General en Tablet: Centrada para títulos, pero... */
        .leader-content-col { text-align: center; }
        /* ...forzamos izquierda en párrafos largos para legibilidad */
        .lead-bio, .bio-block p { text-align: left; } 
        
        .bio-block h5 { justify-content: center; } /* Títulos de bloques centrados se ven bien */
        .bio-block p { margin-left: 0; margin-top: 0.5rem; }
        .badges-row { justify-content: center; }

        /* RESET TOTAL DE BADGES FLOTANTES EN MOBILE */
        .leader-float-badge.left, .leader-float-badge.right {
            position: relative; 
            inset: auto; 
            transform: none !important;
            animation: none; 
            margin: 0;
        }
        
        /* Contenedor de badges debajo de la foto */
        .badges-wrapper-responsive {
            display: flex; 
            justify-content: center; 
            gap: 0.8rem; 
            margin-top: -1.5rem; /* Efecto overlap sutil */
            position: relative; 
            z-index: 3; 
            flex-wrap: wrap;
        }
    }

    @media (max-width: 768px) {
        /* Ajustes finos para Celulares */
        .avatar-photo-giant { height: 380px; }
        
        .leader-specs-box { 
            text-align: left; /* Lista de skills alineada a la izquierda siempre */
            padding: 1.5rem;
        }
        
        .leader-name-block h3 { font-size: 2rem; }
        
        /* Ajuste de badges para que quepan en pantallas muy angostas */
        .leader-float-badge {
            font-size: 0.8rem;
            padding: 0.6rem 1rem;
        }
        
        /* En celulares, alineamos todo a la izquierda para estructura más limpia */
        .leader-content-col { text-align: left; }
        .bio-block h5 { justify-content: flex-start; }
        .badges-row { justify-content: flex-start; }
        
        .leader-header-mobile { text-align: left; }
    }
</style>